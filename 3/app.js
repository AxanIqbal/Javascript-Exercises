let person_name = "ahsan";

console.log(`Lower Case: ${person_name.toLowerCase()}`);
console.log(`Upper Case: ${person_name.toUpperCase()}`);
console.log(
  `Title Case: ${person_name[0].toUpperCase() + person_name.substring(1)}`
);
